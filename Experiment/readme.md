运行test.py，备份result数据到topo_num_result目录中
小中大规模依次增长为:topo_1、topo2、topo_4、topo_3(在实验中会手动调换顺序，后续如果规模和顺序对应上，应该注意修改一下)


完成不同相似度阈值验证实验，随机生成不同相似度区间下的混淆拓扑，呈现对比


mininet部署的吞吐量影响实验，首先实现在没有部署之前的吞吐量测量，然后实现部署之后的吞吐量测量，
部署时直接让s0延迟数据包，将10%的数据包延迟部署的总时间（10000次的costdelay），测吞吐量。
控制器分为部署前(my_learning)、部署后(my_delay)，先运行部署前的测量，再重开运行部署后的测量，需要修改topo_num参数
配合mininet的measure函数。