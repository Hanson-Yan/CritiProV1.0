1、结构相似性计算采用拓扑网络肖像散度的方式，考虑的是结构上的相似性。
2、优化问题，双目标min(关键性)+min(扰动成本)，硬约束：（1）关键元素受保护；（2）连通；软约束（1）结构相似度在0.6~0.9之间；（2）具有部署的物理合理性（虚拟链路延迟大于物理链路）。
3、限制扰动范围在b跳内为工程实现，与拓扑结构相似度高度相关联。
4、结构相似不等于部署成本低，即使是结构相似的拓扑也会因为节点的连接关系不同而导致部署成本增加。